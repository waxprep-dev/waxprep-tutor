# In the processWithAI function, change the profileObj creation:
# Instead of just setting learningStyle: 'adaptive', use a type assertion.

# Find the line with "learningStyle: 'adaptive'" and change it to:
# learningStyle: 'adaptive' as 'adaptive',

# And for the recentTurns, add id and timestamp when mapping:
# recentTurns: context.recentTurns.map(t => ({
#   id: t.id || `turn_${Date.now()}`,
#   role: t.role as 'user' | 'assistant',
#   content: t.content,
#   timestamp: t.timestamp || Date.now(),
# })) || [],
