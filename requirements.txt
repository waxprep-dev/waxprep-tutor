# WaxPrep AI Brain — Python Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# HTTP client for Modal API calls
httpx>=0.27.0

# CUGA (when available — install separately)
# pip install cuga
# OR for development:
# git clone https://github.com/cuga-project/cuga-agent.git
# cd cuga-agent && uv sync && pip install -e .

# For Modal deployment (deploy separately)
# modal>=0.63.0

# Data validation
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0
structlog>=24.1.0

# Optional: LangGraph for advanced agent workflows
# langgraph>=0.0.50
# langchain>=0.1.0

# Optional: For local model testing (without Modal)
# transformers>=4.40.0
# torch>=2.3.0
# accelerate>=0.28.0
